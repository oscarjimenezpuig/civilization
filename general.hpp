#include <iostream>
#include <random>
#include <vector>

using u1 = unsigned char;
using u2 = unsigned short;
using u3 = unsigned int;
using s1 = signed char;
using s2 = signed short;
using s4 = signed int;

double uniform(double min,double max);
//valor aleatorio de distribucion uniforme entre min y max

double normal(double media,double desviacio);
//valor aleatorio siguiendo la media y desviacion
